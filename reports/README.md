# Seed batch mapping reports (August 2026)

Authoring reports for the eight seed entries, moved here from
`entries/<slug>/summary.md` — the entry directory is the published artifact
(docs/SPEC.md defines its layout), and these are addressed to the sightmap
team, not to entry consumers.

One file per entry. `apple.md` doubles as the closing report for the whole
batch: candidate-site rejection rates, the requests re-capture retrospective,
and the tooling/skill asks distilled from all eight mappings.
