{
  "name": "@sightmap/atlas-scripts",
  "private": true,
  "description": "CI validation and index-generation scripts for the sightmap atlas.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate-entry": "node scripts/validate-entry.mjs",
    "gen-index": "node scripts/gen-index.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "image-size": "^2.0.2",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "pngjs": "^7.0.0"
  }
}
