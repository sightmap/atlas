# Cross-route components and requests (reachable from more than one route).
version: 1

components:
  - name: Navigation
    selector: 'nav[data-component="Navigation"]'

  - name: Footer
    selector: 'footer[data-component="Footer"]'

requests:
  - name: GetCurrentUser
    route: /api/me
    method: GET
